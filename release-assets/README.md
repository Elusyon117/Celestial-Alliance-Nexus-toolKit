# Temporary release assets

Do not commit the 4.9 SCMDB source JSON into this directory. Upload it to a temporary GitHub prerelease as documented in `docs/SCMDB_4_9_IMPORT.md`. The workflow converts it into the tracked normalized files under `data/`.
