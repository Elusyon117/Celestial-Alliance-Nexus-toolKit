# Restore stable version

Delete everything in the local repo folder except `.git`, then extract this package into the folder and commit the result.

Keep only the asset folders included here unless you intentionally add new local assets later.
